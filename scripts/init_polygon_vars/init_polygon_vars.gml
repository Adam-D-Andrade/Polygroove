/// @description init_polygon_vars(points, radius, thickness, rotation)
/// @function draw_polygon
/// @param points
/// @param radius
/// @param thickness
/// @param rotation
/// @description Sets up a polygon to draw with n number of sides using a thickness, you can even create a solid
    //polygon by using a thickness the same as the radius. Radius is the overall size of polygon.
	//Thickness is thickness of outer line.

points = argument0;
radius = argument1;
thickness = argument2;
rotation = argument3;
